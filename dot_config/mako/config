# Config items that are set to their defaults are commented out
#anchor=top-right
# Display in all cases except when something is full screen
#layer=top
font=FontAwesome 10
border-radius=10
default-timeout=3000
group-by=summary
# Enable icons in mako configuration
#icons=1
# This is an example of how to add a sound effect to your notifications
#on-notify=exec mpv ~/.config/mako/sounds/Tink.aiff

[grouped]
format=<b>%s</b>\n%b
# vim: set ft=ini
